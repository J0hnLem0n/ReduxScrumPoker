const initialState = {
    cards: [{id:1}, {id:3}, {id:5}, {id:8}, {id:13}]
}

export default function desk(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
